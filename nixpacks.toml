# Nixpacks configuration para Next.js en Railway
# Optimizado para despliegue en producción
# Usar Node.js 20 en Nixpacks

[phases.setup]
nixPkgs = ["nodejs_20"]

# Nixpacks detectará automáticamente install, build y start desde `frontend/package.json`
